package com.minkang.ultimate.managers;

import org.bukkit.event.Listener;

/**
 * Compile-safe stub; the real /거래 구현은 usp2 패키지에 있습니다.
 * 이 클래스는 빌드 에러를 막기 위한 빈 Listener 입니다.
 */
public class TradeManager implements Listener {
    public TradeManager(Object ignored) { }
}
